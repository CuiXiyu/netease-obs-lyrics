# 更新日志

## v0.1.2 - 2026-06-03

### 改进

- 将网页、设置页、服务端提示和常用脚本输出改为中文。
- PowerShell 脚本改为带 BOM 的 UTF-8，避免 Windows PowerShell 5.1 在新电脑上把中文提示读成乱码。
- 逐字歌词继续使用网易云真实逐字时间轴染色。
- 逐句歌词和普通 LRC 歌词改为像网易云一样按整句染色，不再估算逐字进度。

### 测试

- 已验证 PowerShell 5.1 能正常运行状态检查脚本并输出中文。

## v0.1.1 - 2026-06-03

### 修复

- 修复 RAD DOGS 等歌曲播放中途网易云 `lastPlaying` 进度源短暂异常时，歌词画面可能变成“无可靠进度”的问题。
- BetterNCM 桥接新增原生 `getPlaying` / `getPlayingSong` 读取尝试，并在诊断信息中记录返回摘要，方便后续适配。
- 服务端不再让 Windows MediaSession 的无可靠进度数据覆盖已有的可靠 BetterNCM 桥接进度。
- 当桥接明确上报“正在播放”时，服务端不会仅因为同一进度值短时间不动就误判为暂停。

### 测试

- 已在 RAD DOGS 上测试 20 秒连续播放，歌词状态保持 `ready`，模式为 `word`，进度从 34s 连续递增到 53s，未再出现无可靠进度提示。

## v0.1.0 - 2026-05-30

首次可用版本。

### 新增

- OBS 浏览器源歌词显示页面。
- BetterNCM 桥接插件，用于读取网易云音乐当前歌曲、歌词和真实播放进度。
- 本地歌词服务，默认地址为 `http://127.0.0.1:47863`。
- 设置页面，支持字体、字号、对齐、扫色颜色、未扫色颜色、透明度和阴影强度调整。
- 自动扫描系统字体，并优先显示可识别的中文字体名。
- 一键安装和启动脚本：`Setup and Start.cmd`、`Start OBS Lyrics.cmd`。
- 服务状态检查脚本：`Check Status.cmd`、`health.ps1`。
- 服务关闭脚本：`关闭 OBS 歌词服务.cmd`、`stop-service.ps1`。
- 发布包打包脚本：`scripts/package-release.ps1`。

### 修复

- 修复歌词扫色在窄宽度换行时可能串到下一行的问题。
- 修复暂停时歌词可能继续滚动的问题。
- 修复播放状态被网易云内部 `status: 0` 字段误判为暂停的问题。
- 修复部分歌曲歌词接口返回空内容时无法 fallback 到旧 LRC 参数的问题，例如 `朝露人间`。
- 修复启动脚本缺少稳定服务日志导致新机启动失败难以定位的问题。

### 已知问题

- BetterNCM 插件安装或更新后，通常需要重启网易云音乐。
- 网易云 PC 客户端或 BetterNCM 更新后，内部字段变化可能需要重新适配。
- 当前自动化测试覆盖有限，暂停、拖动进度条、换歌和歌词格式兼容性仍建议人工复测。
